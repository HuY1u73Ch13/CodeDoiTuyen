#include<bits/stdc++.h>
const int L[]={2,3,4,1};
const int R[]={4,3,2,1};
const int B[]={3,2,1,4};
#define ll long long
using namespace std ;
ll a,x=0,y=0,h=0;
string b;
int main ()
{
    cin>>a>>b;
    for(int i=0;i<=b.size();i++)
    {
        if(b[i]=='L') h=L[h];
        if(b[i]=='L') h=L[h];
        if(b[i]=='L') h=L[h];
        x+=
    }
}
